# Unused

This directory is not referenced by anything. The slide-7 proof tiles live at
`/welcome/assets/` and are shared by every referrer page; see the README there.

An old `onError` fallback chain used to reach for `/assets/referral/*.png`.
That chain was removed, so nothing resolves here any more.
