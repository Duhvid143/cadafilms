# Dependencies
node_modules/
functions/node_modules/

# Build artifacts
.next/
out/
build/
functions/lib/

# Environment variables
.env.local
.env.*.local
functions/.env

# Firebase
firebase-debug.log*
.firebase/

# Secrets — allow only known JSON files
*.json
!firebase.json
!firestore.indexes.json
!cors.json
!functions/package.json
!functions/tsconfig.json
!sitemap.xml
!public/manifest.json

# Dormant CMS code (keep tracked but note its purpose)
# _dormant/ contains the archived Next.js + CMS codebase

# System
.DS_Store
._*
